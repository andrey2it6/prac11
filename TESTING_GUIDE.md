#!/bin/bash
# TESTING_GUIDE.md - Полное руководство по тестированию

# # 🧪 РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ СИСТЕМЫ

# Этот документ показывает как протестировать ВСЕ критерии оценки

# ## 📋 БЫСТРЫЙ СТАРТ (5 минут)

# ```bash
# # 1. Запустить систему
# docker-compose up --build -d

# # 2. Подождать инициализации (~30 секунд)
# sleep 30

# # 3. Запустить встроенные тесты (демонстрируют ВСЕ критерии)
# docker-compose exec node npm test

# # 4. Открыть браузер
# http://localhost:3000
# ```

# ## ✅ КРИТЕРИЙ 1: Работоспособность (30%)

# ### 1.1 Health Checks

# ```bash
# # Node.js статус
# curl http://localhost:3000/health

# # PHP статус
# curl http://localhost:8001/health.php
# ```

# ### 1.2 Фронтенд доступен

# ```bash
# # HTML страница загружается
# curl -s http://localhost:3000 | head -20
# ```

# ## ✅ КРИТЕРИЙ 2: Интеграция (30%)

# ### 2.1 SOAP → REST преобразование

# ```bash
# # Node.js получает данные через SOAP, возвращает JSON
# curl "http://localhost:3000/api/physical/books?author=Толстой" | jq .

# # Ожидаемый результат:
# # {
# #   "success": true,
# #   "count": 1,
# #   "books": [
# #     {
# #       "inventory_number": "LIB-2024-002",
# #       "title": "Война и мир",
# #       "author": "Лев Толстой",
# #       "status": "available"
# #     }
# #   ]
# # }
# ```

# ### 2.2 SQLite транзакции (выдача книги)

# ```bash
# # Проверим статус книги ДО
# curl "http://localhost:3000/api/physical/books?inventory=LIB-2024-005"

# # Выдаём книгу
# curl -X POST http://localhost:3000/api/physical/loan \
#   -H "Content-Type: application/json" \
#   -d '{
#     "inventory_number": "LIB-2024-005",
#     "reader_card": "R-12345"
#   }' | jq .

# # Проверим статус ПОСЛЕ - должен быть 'borrowed'
# curl "http://localhost:3000/api/physical/books?inventory=LIB-2024-005"
# ```

# ### 2.3 Возврат книги

# ```bash
# # Возвращаем книгу
# curl -X POST http://localhost:3000/api/physical/return \
#   -H "Content-Type: application/json" \
#   -d '{"inventory_number": "LIB-2024-005"}' | jq .

# # Статус должен вернуться в 'available'
# curl "http://localhost:3000/api/physical/books?inventory=LIB-2024-005"
# ```

# ### 2.4 TinyDB цифровые ресурсы

# ```bash
# # Получить все ресурсы из TinyDB
# curl http://localhost:3000/api/digital/resources | jq .

# # Ожидаемый результат: 5 книг, аудиокниг и статей
# ```

# ### 2.5 Логирование скачивания (TinyDB запись)

# ```bash
# # Залогировать скачивание
# curl -X POST http://localhost:3000/api/digital/download \
#   -H "Content-Type: application/json" \
#   -d '{"resourceId": "1", "userId": "test-user"}' | jq .

# # Проверить что счетчик увеличился
# curl http://localhost:3000/api/digital/resources | jq '.resources[0].downloadCount'
# ```

# ### 2.6 XML парсинг отчета

# ```bash
# # Получить отчет в виде JSON (из XML)
# curl http://localhost:3000/api/internal/overdue-report | jq .

# # Должны видеть список просроченных/активных книг
# ```

# ## ✅ КРИТЕРИЙ 3: Качество кода (20%)

# ### 3.1 Обработка ошибок

# ```bash
# # Неверные параметры поиска
# curl "http://localhost:3000/api/physical/books"
# # Ошибка: "Укажите параметр \"author\" или \"inventory\""

# # Невалидный читательский билет (слишком короткий)
# curl -X POST http://localhost:3000/api/physical/loan \
#   -H "Content-Type: application/json" \
#   -d '{"inventory_number": "LIB-2024-001", "reader_card": "X"}'
# # Ошибка: "Некорректный номер читательского билета"

# # Попытка выдать уже выданную книгу
# curl -X POST http://localhost:3000/api/physical/loan \
#   -H "Content-Type: application/json" \
#   -d '{"inventory_number": "LIB-2024-004", "reader_card": "R-TEST-001"}'
# # Уже выдана - должна быть ошибка
# ```

# ### 3.2 Модульная архитектура

# ```bash
# # Проверить структуру файлов
# tree nodejs/
# # Видим разделение:
# # - server.js (точка входа)
# # - config.js (конфигурация)
# # - routes/ (маршруты)
# # - utils/ (утилиты - SOAP клиент)
# # - db/ (база данных - TinyDB)
# # - public/ (фронтенд)
# # - tests/ (тесты)
# ```

# ## ✅ КРИТЕРИЙ 4: Полнота технологий (20%)

# ### 4.1 WSDL схема

# ```bash
# # Получить WSDL для SOAP сервиса
# curl -s "http://localhost:8001/soap-server.php?wsdl" | grep -i 'definitions'
# # Должны видеть XML WSDL схему с операциями
# ```

# ### 4.2 XSLT преобразование

# ```bash
# # Получить HTML отчет (с XSLT преобразованием)
# curl -s "http://localhost:8001/report.php?type=overdue" | grep -i 'table'
# # Должны видеть HTML таблицу с красивым стилем
# ```

# ### 4.3 SOAP операции

# ```bash
# # Проверить что SOAP сервер доступен
# curl -s "http://localhost:8001/soap-server.php?wsdl" | grep -i 'operation'
# # Должны видеть 4 операции:
# # - getBookByInventory
# # - searchBooksByAuthor
# # - registerLoan
# # - returnBook
# ```

# ### 4.4 REST API полнофункционален

# ```bash
# # Все REST эндпоинты работают
# curl http://localhost:3000/api/physical/books?author=Test
# curl http://localhost:3000/api/digital/resources
# curl http://localhost:3000/api/digital/stats
# curl http://localhost:3000/api/internal/overdue-report
# ```

# ### 4.5 Обе БД используются

# ```bash
# # SQLite для физических книг
# curl "http://localhost:3000/api/physical/books?author=Булгаков" | jq '.books | length'

# # TinyDB для цифровых ресурсов
# curl http://localhost:3000/api/digital/resources | jq '.resources | length'
# ```

# ## 📊 ПОЛНОЕ ТЕСТИРОВАНИЕ

# ### Встроенные тесты (РЕКОМЕНДУЕТСЯ)

# ```bash
# # Запускает все критерии автоматически
# docker-compose exec node npm test

# # Вывод:
# # 📊 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
# # ============================================================
# # ✅ Пройдено: 30
# # ❌ Ошибок: 0
# # 📈 Всего: 30
# # 🎯 Успешность: 100%
# # ============================================================
# ```

# ### Postman коллекция

# ```bash
# # 1. Скачайте Postman (https://www.postman.com/)
# # 2. Импортируйте тесты/postman-collection.json
# # 3. Запустите всю коллекцию (Run Collection)
# # 4. Результаты покажут статус каждого теста
# ```

# ### Ручное тестирование

# ```bash
# # Откройте веб-интерфейс
# # http://localhost:3000

# # Тестируйте через браузер:
# # 1. Вкладка "Физические книги" - поиск и выдача
# # 2. Вкладка "Цифровые ресурсы" - скачивание
# # 3. Вкладка "Admin-панель" - просмотр отчетов
# # 4. Вкладка "Статус системы" - проверка здоровья
# ```

# ## 🐛 TROUBLESHOOTING

# ### Порты заняты

# ```bash
# # Если 3000 или 8001 заняты
# netstat -ltn | grep LISTEN  # Linux/Mac
# netstat -ano | findstr LISTENING  # Windows

# # Или измените порты в docker-compose.yml
# ```

# ### Контейнеры не стартуют

# ```bash
# # Проверьте логи
# docker-compose logs -f

# # Перестройте без кэша
# docker-compose down -v
# docker-compose up --build --force-recreate -d
# ```

# ### Сервисы недоступны

# ```bash
# # Проверьте что Docker сеть создана
# docker network ls | grep library

# # Проверьте подключение контейнеров
# docker-compose ps
# ```

# ## 📈 КРИТЕРИИ УСПЕХА

# ✅ **30% Работоспособность**
#   - Все сервисы запущены
#   - Фронтенд доступен
#   - API отвечает

# ✅ **30% Интеграция**
#   - SOAP ↔ REST работает
#   - SQL транзакции работают
#   - TinyDB операции работают
#   - XML парсинг работает

# ✅ **20% Качество кода**
#   - Модульная архитектура
#   - Обработка ошибок
#   - Валидация данных
#   - Логирование

# ✅ **20% Полнота технологий**
#   - WSDL присутствует
#   - SOAP сервер работает
#   - XML/XSLT используется
#   - REST API полнофункционален
#   - Обе БД используются

# ## 🚀 ДЕМО-СЦЕНАРИИ

# ### Сценарий 1: Поиск и выдача физической книги

# ```bash
# # 1. Поиск
# curl "http://localhost:3000/api/physical/books?author=Пушкин" | jq .

# # 2. Выдача
# curl -X POST http://localhost:3000/api/physical/loan \
#   -H "Content-Type: application/json" \
#   -d '{"inventory_number":"LIB-2024-005","reader_card":"R-DEMO-001"}'

# # 3. Проверка статуса
# curl "http://localhost:3000/api/physical/books?inventory=LIB-2024-005"

# # 4. Возврат
# curl -X POST http://localhost:3000/api/physical/return \
#   -H "Content-Type: application/json" \
#   -d '{"inventory_number":"LIB-2024-005"}'
# ```

# ### Сценарий 2: Работа с цифровыми ресурсами

# ```bash
# # 1. Получить ресурсы
# curl http://localhost:3000/api/digital/resources | jq '.resources[0]'

# # 2. Скачать ресурс
# curl -X POST http://localhost:3000/api/digital/download \
#   -H "Content-Type: application/json" \
#   -d '{"resourceId":"1","userId":"demo-user"}'

# # 3. Статистика
# curl http://localhost:3000/api/digital/stats | jq '.stats.topDownloaded'
# ```

# ### Сценарий 3: Просмотр отчетов

# ```bash
# # 1. Просроченные книги (JSON)
# curl http://localhost:3000/api/internal/overdue-report | jq '.report.overdueCount'

# # 2. HTML отчет (в браузере)
# # Откройте http://localhost:8001/report.php

# # 3. Raw XML
# curl -s "http://localhost:8001/report.php?format=xml" | head -20
# ```

# ## ✨ ГОТОВО!

# Если все тесты прошли успешно - система полностью функциональна
# и демонстрирует ВСЕ критерии оценки.

echo "🧪 Руководство по тестированию создано"
